import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack(spacing: 8) {
            Text("Pallade")
                .font(.headline)
            Text("Spike di provisioning")
                .font(.caption2)
                .foregroundStyle(.secondary)
        }
    }
}
